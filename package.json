{
  "scripts": {
    "build": "tsc --noEmit && tsc --outDir build",
    "start": "npm run build && node build/models/index.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.4"
  }
}
